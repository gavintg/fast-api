#!/usr/bin/env bash

mkdir -p $PWD/db
chmod 777 $PWD/db

docker compose up

cat << EOF > $PWD/.env
MYSQL_USERNAME=root
MYSQL_PASSWORD=password
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DATABASE=fastapi
EOF